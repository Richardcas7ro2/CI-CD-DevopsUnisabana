global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'devops-webapp'
    # Extrae métricas de la app expuesta por Kubernetes (kind) en el host
    static_configs:
      - targets: ['host.docker.internal:30080']
